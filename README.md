# confidence-sampling
Python package for data subsampling and coreset selection using learning dynamics.

## Installing

Activate venv with `source ~/venvs/biovenv/bin/activate` and then run:
```cmd
pip uninstall confidence-sampling  # If confidence-sampling already installed
pip install git+https://github.com/Yam-Arieli/confidence-sampling.git
```

